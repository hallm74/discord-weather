# Discord webhook URL (required)
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your/webhook

# Coordinates (optional; defaults provided in code)
LAT=31.565
LON=-93.484

# Optional display name for the location
LOCATION_NAME=Many, LA

# Set to true to enable GitHub Models running-layer advice
ENABLE_AI=false

# GH_MODELS_TOKEN is automatically provided in GitHub Actions via secrets.GITHUB_TOKEN.
# For local testing with ENABLE_AI=true, use a personal access token with Models access.
GH_MODELS_TOKEN=
